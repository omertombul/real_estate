import { BuyContent } from "@/components/BuyContent";

export const metadata = {
  title: "Acheter / Buy | Teresa Real Estate · Montréal",
  description:
    "Looking to buy a home in Montréal? Teresa offers expert buyer representation, access to exclusive listings, and full support from search to keys.",
};

export default function BuyPage() {
  return <BuyContent />;
}
